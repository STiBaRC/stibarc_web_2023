# Config persistent storage
apiVersion: v1
kind: PersistentVolume
metadata:
  name: stibarc-web-dev-config-storage
spec:
  capacity:
    storage: 512Mi
  accessModes:
    - ReadWriteMany
  mountOptions:
    - nfsvers=4.1
  nfs:
    path: /srv/kubernetes/stibarc-web-dev-config
    server: 10.0.0.8
  persistentVolumeReclaimPolicy: Retain

---
# Config persistent volume claim
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stibarc-web-dev-config-storage-claim
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 512Mi
  volumeName: stibarc-web-dev-config-storage

---
# Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stibarc-web-dev
  labels:
    app.kubernetes.io/name: stibarc-web-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: stibarc-web-dev
  template:
    metadata:
      labels:
        app.kubernetes.io/name: stibarc-web-dev
    spec:
      containers:
      - name: stibarc-web-dev
        image: registry.stibarc.com/stibarc-web:latest
        ports:
        - containerPort: 80
        volumeMounts:
        - mountPath: /var/www/web/config
          name: stibarc-web-dev-config-storage
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
            successThreshold: 1
      restartPolicy: Always
      imagePullSecrets:
      - name: regcred
      volumes:
      - name: stibarc-web-dev-config-storage
        persistentVolumeClaim:
          claimName: stibarc-web-dev-config-storage-claim

---
# Service
apiVersion: v1
kind: Service
metadata:
  name: stibarc-web-dev
spec:
  selector:
    app.kubernetes.io/name: stibarc-web-dev
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80

---
# Ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stibarc-web-dev
spec:
  rules:
  - host: dev.stibarc.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: stibarc-web-dev
            port:
              number: 80